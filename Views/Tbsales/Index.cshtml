@model IEnumerable<Test.Models.DB.Tbsale>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Sales</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SalesName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SalesName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Guidsales">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Guidsales">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Guidsales">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
